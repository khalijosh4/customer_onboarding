version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: khalijosh1212
      POSTGRES_DB: fortune_sacco_onboarding
    ports:
      - '5432:5432'
    volumes:
      - fortune_sacco_pgdata:/var/lib/postgresql/data

volumes:
  fortune_sacco_pgdata:
